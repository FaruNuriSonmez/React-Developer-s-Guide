import React,{useRef} from "react";

export type Props = {
    open: boolean;
    setOpen: (v:boolean) => void;
}

const Menu = (props: Props) => {
    return(
        <>
        </>
    )
};

export default Menu;